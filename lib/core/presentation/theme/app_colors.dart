import 'package:flutter/material.dart';

/// 앱 전체에서 사용되는 색상 정의
class AppColors {
  /// 주 색상 (Primary)
  static const Color primary = Color(0xFF5C6BC0);  // Indigo
  
  /// 보조 색상 (Secondary)
  static const Color secondary = Color(0xFF26A69A);  // Teal
  
  /// 강조 색상 (Accent)
  static const Color accent = Color(0xFFFF4081);  // Pink
  
  /// 배경 색상 (Light)
  static const Color background = Color(0xFFF5F5F5);  // Light Gray
  
  /// 텍스트 색상 (Dark)
  static const Color textDark = Color(0xFF333333);  // Dark Gray
  
  /// 텍스트 색상 (Light)
  static const Color textLight = Color(0xFFFFFFFF);  // White
  
  /// 성공 색상 (Success)
  static const Color success = Color(0xFF4CAF50);  // Green
  
  /// 경고 색상 (Warning)
  static const Color warning = Color(0xFFFFC107);  // Amber
  
  /// 오류 색상 (Error)
  static const Color error = Color(0xFFF44336);  // Red
  
  /// 비활성화 색상 (Disabled)
  static const Color disabled = Color(0xFFBDBDBD);  // Gray
  
  /// 카드 배경 색상
  static const Color cardBackground = Color(0xFFFFFFFF);  // White
  
  /// 구분선 색상
  static const Color divider = Color(0xFFE0E0E0);  // Light Gray
  
  /// 아이콘 색상
  static const Color icon = Color(0xFF757575);  // Gray
  
  /// 팝스(PAPS) 테마 색상
  static const Color papsTheme = Color(0xFF3F51B5);  // Indigo
  
  /// 줄넘기 테마 색상
  static const Color jumpRopeTheme = Color(0xFF00BCD4);  // Cyan
}